---
interface Props {
  label: string;
  dark?: boolean;
  class?: string;
}
const { label, dark = false, class: className = '' } = Astro.props;
---

<div class:list={['ph', { 'ph-dark': dark }, className]} data-label={label}></div>
